{
  "name": "notes-app",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --force --prefix backend && npm install --force --prefix frontend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend" ,
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
